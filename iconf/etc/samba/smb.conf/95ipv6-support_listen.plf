#!/usr/bin/perl -p -CSDAL

my @ips = sort split "\n", qx(sh -c 'netquery6 --global --lan --format "ip"; netquery6 --uniquelocal --lan --format "ip"');
push @ips, "::1";
my $out = join " ", @ips;

s/interfaces = (.*)$/interfaces = $1 $out/g;
